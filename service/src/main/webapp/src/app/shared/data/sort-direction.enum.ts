/**
 * Enumerate the sort directions that Spring Data expects.
 */
export enum SortDirection {
  /**
   * Sort in ascending order
   */
  Ascending = "ASC",

  /**
   * Sort in descending order
   */
  Descending = "DESC"
}
