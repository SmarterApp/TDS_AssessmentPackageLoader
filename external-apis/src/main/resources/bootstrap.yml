spring:
  application:
    name: tds-support-tool-external-service
  cloud:
    config:
      uri: ${CONFIG_SERVICE_URL:http://localhost:8008}
      enabled: ${CONFIG_SERVICE_ENABLED:false}
      fail-fast: ${CONFIG_SERVICE_ENABLED:false}